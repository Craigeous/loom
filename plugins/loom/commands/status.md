---
description: Print a summary of loom's .docs/ state for this repo
argument-hint:
---

# /loom:status

Load and follow the `loom-status` skill at
`${CLAUDE_PLUGIN_ROOT}/skills/loom-status/SKILL.md` exactly, treating its
root-relative references as relative to `${CLAUDE_PLUGIN_ROOT}`. Scan only — no
agents, no writes.
