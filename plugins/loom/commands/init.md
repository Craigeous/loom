---
description: Initialize or align this repo to loom's conventions
argument-hint:
---

# /loom:init

Load and follow the `loom-init` skill at
`${CLAUDE_PLUGIN_ROOT}/skills/loom-init/SKILL.md` exactly, treating its
root-relative references as relative to `${CLAUDE_PLUGIN_ROOT}`.
