{
  "description": "Block git identity overrides (ADR 0003) and enforce the write-ahead compaction invariant (ADR 0013). One shared manifest for both Claude and Codex (spec 10): the command resolves the hook executable from PLUGIN_ROOT (Codex) falling back to CLAUDE_PLUGIN_ROOT (Claude), evaluated only inside the spawned hook process; each hook script then validates which of those two roots is a real installed loom plugin root and selects its client wire adapter accordingly (spec 08).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}/hooks/git-identity-guard.sh\""
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}/hooks/precompact-write-ahead-backstop.sh\""
          }
        ]
      }
    ]
  }
}
